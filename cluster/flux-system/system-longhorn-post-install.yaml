apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: system-longhorn-post-install
  namespace: flux-system
spec:
  interval: 2m0s
  path: ./components/longhorn/post-install
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  # dependsOn:
  #   - name: system-longhorn
  #   - name: system-traefik
  wait: true
